export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // TODO: basic home path
  BASE_HOME = '/role/log',
  // error page path
  ERROR_PAGE = '/exception',
  // auth2登录路由路径
  OAUTH2_LOGIN_PAGE_PATH = '/oauth2-app/login',
}
